# Operator runbook

Run `npm ci` explicitly in both package directories, then review/apply `backend/migrations/001_knowledge_workflow.sql` with `DATABASE_URL=... ./scripts/migrate.sh`. Startup is read-only and never installs, seeds, migrates, drops tables, starts PostgreSQL, or kills ports. Configure a unique 32+ character JWT secret and leave experimental routes disabled.

Unresolved citations, stale/conflicting evidence, or unauthorized documents force abstention. Legal holds block deletion. Object stores, repositories, indexes, Shopify/WooCommerce, and embedding providers remain fail-closed until credentials, signed webhook verification, resumable contract fixtures, and deletion receipts exist. Quarantine suspected prompt-injection content; do not index it as instructions.
